import _ from 'lodash-es';

// 示例：使用 _.merge 合并对象
const obj1 = { a: 1, b: 2 };
const obj2 = { b: 3, c: 4 };
const merged = _.merge(obj1, obj2);
// console.log(merged); // 输出：{ a: 1, b: 3, c: 4 }

// export{
//     merged
// }
export default{
    merged
}